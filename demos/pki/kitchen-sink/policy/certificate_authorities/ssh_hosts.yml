- !policy
  id: ca/ssh-hosts
  body:
    - !variable private-key
    - !variable public-key

    # To grant signing entitlement for intermediate CA
    - !host

    - !webservice
      annotations:
        ca/private-key: conjur/ca/ssh-hosts/private-key
        ca/certificate-chain: conjur/ca/ssh-hosts/public-key
        ca/max-ttl: P1M

    - !group clients

    - !permit
      role: !group clients
      privilege: [ sign ]
      resource: !webservice