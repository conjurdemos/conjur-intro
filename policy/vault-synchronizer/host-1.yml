# Host for Synchronizer
- !host synchronizer-1

# Add Synchronizer host to the `members` group (so this host can update the LOB last updated variables)
- !grant
  member: !host synchronizer-1
  role: !group conjur/vault-synchronizer/members
